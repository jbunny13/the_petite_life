<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <span class="cyan-text">The</span> 
        <span class="black-text">Petite </span>
        <span class="pink-text">Life</span>
      </a>
    </div><!-- /navbar-header -->

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-links">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'All Categories', categories_path %></li>
            <% Category.all.each do |category| %>
              <li><%= link_to category.name, category_path(category) %></li>
            <% end %>
          </ul>
        </li>
        
        <li><%= active_link_to 'Articles', articles_path, wrap_tag: 'li', active: :exclusive %></li>
        <li><%= active_link_to 'Products', products_path, wrap_tag: 'li', active: :exclusive %></li>
        <li><%= active_link_to 'Resources', references_path, wrap_tag: 'li', active: :exclusive %></li>
      </ul>
      
      <div class="pull-right">
        <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <li><%= active_link_to current_user.full_name, edit_user_registration_path, wrap_tag: 'li', active: :exclusive %></li>
            <li><%= active_link_to "Log Out", destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= active_link_to "Sign Up", new_user_registration_path, wrap_tag: 'li', active: :exclusive %></li>
            <li><%= active_link_to "Log In", new_user_session_path, wrap_tag: 'li', active: :exclusive  %></li>
          <% end %>
        </ul>
      </div><!-- /.signup-login -->
    </div><!-- /.navbar-collapse -->
    
    <div class="navbar search pull-right">
      <%= form_tag searches_path, method: :get, class: 'form-inline', html: { role: 'search' } do %>
        <%= text_field_tag :query, params[:query], class: 'form-control', size: 30, maxlength: 30 %>
        <button type="submit" class="btn btn-default">
          <span class="fa fa-search"></span>
        </button>
      <% end %>
    </div><!-- /.search -->
  </div><!-- /.container-fluid -->
</nav>
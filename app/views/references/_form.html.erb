<%= form_for(@reference, html: { role: 'form'}) do |f| %>
  <% if @reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reference.errors.count, "error") %> prohibited this reference from being saved:</h2>

      <ul>
      <% @reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name, class: 'form-control input-lg' %>
    </div>
    
    <div class="field">
      <%= f.label :uri %><br>
      <%= f.text_field :uri, class: 'form-control input-lg' %>
    </div>

    <div class="field">
      <%= f.label :user_id %><br>
      <p class="form-control-static"><%= current_user.full_name %></p>
    </div>

    <%= f.submit 'Save', class: 'btn btn-default' %>
  </div>
<% end %>

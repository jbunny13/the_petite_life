$('#references').html('<%= escape_javascript render @references %>');
$('.paginate').html('<%= escape_javascript(paginate(@references, remote: true).to_s) %>');
$('.num_entries').html('<%= escape_javascript(page_entries_info(@references, entry_name: 'resource', remote: true).to_s) %>');
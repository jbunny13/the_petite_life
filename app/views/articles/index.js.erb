$('#articles').html('<%= escape_javascript render @articles %>');
$('.paginate').html('<%= escape_javascript(paginate(@articles, remote: true).to_s) %>');
$('.num_entries').html('<%= escape_javascript(page_entries_info(@articles, remote: true).to_s) %>');
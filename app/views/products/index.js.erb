$('#products').html('<%= escape_javascript render @products %>');
$('.paginate').html('<%= escape_javascript(paginate(@products, remote: true).to_s) %>');
$('.num_entries').html('<%= escape_javascript(page_entries_info(@products, remote: true).to_s) %>');

$('.star-rating').raty({
  path: '',
  readOnly: true,
  score: function() { return $(this).attr('data-score') }
});